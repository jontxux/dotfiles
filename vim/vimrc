" Para que Vim se comporte como se espera en otros editores
set nocompatible

" Habilitar el portapapeles del sistema
set clipboard=unnamedplus,unnamed

" Mostrar números de línea relativos
set relativenumber

" Mostrar el número de línea actual
set number

" Habilitar soporte para el mouse en todos los modos
set mouse=a

" Búsquedas que ignoran mayúsculas y minúsculas, a menos que se use una mayúscula
set ignorecase
set smartcase

" Resaltar búsquedas
set hlsearch

" Hacer que la búsqueda sea más dinámica
set incsearch

" Permitir el uso de la tecla backspace en todos los modos
set backspace=indent,eol,start

" Activar el uso de 256 colores, útil para temas y sintaxis
set t_Co=256

" Mejorar la interfaz de autocompletado
set wildmenu
set wildmode=full
set wildoptions=pum

" Mostrar una barra de comandos que se expande a medida que se escribe
" set cmdheight=2

" Añadir un pequeño margen en la parte superior e inferior de la ventana
set scrolloff=5

" Asegurarse de que Vim lee y escribe archivos con codificación UTF-8
set encoding=utf-8
set fileencoding=utf-8

" Habilitar el uso de la rueda del mouse en Vim
set mouse=a

" Hacer que Vim sea más receptivo
set ttyfast

" Habilitar el guardado y cargado automático del historial de comandos, búsqueda, etc.
set history=1000

" Realzar la sintaxis, si está disponible
if has("syntax")
  syntax on
endif

" Configurar los espacios por tabulación y la indentación automática
set tabstop=4
set shiftwidth=4
set expandtab
set autoindent
set smartindent

set laststatus=2
set statusline=%f\ [%Y]\ %h%m%r%=%l,%c\ %P

" Ocultar buffers no utilizados en lugar de cerrarlos
set hidden

" Mostrar la línea de comando con información útil
set ruler

" Mostrar coincidencias mientras se escribe la búsqueda
set incsearch

" Permitir el cambio entre archivos sin guardar
set autowriteall

" Resaltar la línea donde se encuentra el cursor
" set cursorline

" Ajustar cómo se muestran los caracteres de fin de línea y tabulación
set list
set listchars=tab:»·,trail:·,nbsp:·
let &t_SI = "\<Esc>[6 q"
let &t_EI = "\<Esc>[2 q"


" Guardar y restaurar la posición del cursor al abrir archivos
if has("autocmd")
  au BufReadPost * if line("'\"") > 1 && line("'\"") <= line("$") | exe "normal! g'\"" | endif
endif

filetype plugin indent on
